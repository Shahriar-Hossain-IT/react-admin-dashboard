on:
  push:
    branches: main
name: 🚀 Deploy website on push
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v3

    - name: Use Node.js 18
      uses: actions/setup-node@v2
      with:
        node-version: '18'
    - name: 🔨 Build Project & Deploy
      run: |
        npm install
        git remote add origin https://github.com/Shahriar-Hossain-IT/react-admin-dashboard.git
        gh-pages -d build
